# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AdobeTestProject' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # getting error: [!] The 'Pods-AdobeTestProject' target has transitive dependencies that include statically linked binaries: (ACPCore)
  # pod 'AdobeBranchExtension', '~> 0.1'
  pod 'ACPAnalytics', '~> 2.0'
  # pod 'ACPCore', '~> 2.0'
  pod 'ACPUserProfile', '~> 2.0'
  
  pod 'AdobeBranchExtension'

  pod 'ACPGriffon'

end
